export function createNoteDocument({ content, createdAt }) {
  return `# Note

${content}

---
createdAt: ${createdAt}
`;
}

export function createDecisionDocument({ content, createdAt }) {
  return `# Decision

          ${content}

          ## Context

          TODO: describe why this decision was made.

          ## Consequences

          TODO: describe the trade-offs.

          ---
          createdAt: ${createdAt}
          `;
}

export function createResearchDocument({ content, createdAt }) {
  return `# Research

          ${content}

          ## Source

          manual

          ## Notes

          TODO: summarize key findings.

          ---
          createdAt: ${createdAt}
          `;
}

export function createProjectReadmeDocument({ projectName, createdAt }) {
  return `# ${projectName}

          ## Purpose

          TODO: describe project purpose.

          ## Structure

          TODO: describe project structure.

          ---
          createdAt: ${createdAt}
          `;
}

export function createProjectFileDocument({ cellId, createdAt }) {
  return `// TODO: generated by ${cellId}
           // createdAt: ${createdAt}
          `;
}
